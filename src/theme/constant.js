import { Dimensions } from "react-native";
const { width, height } = Dimensions.get("window");

/**
 * This UI Design is based on 1170 x 1170 Screen.(iPhone 12)
 */
 export const unitW = width / 1170;
 export const unitH = height / 2532;